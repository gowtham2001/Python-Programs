def move_zero_to_end(arr):
  count = 0
  
  for i in arr:
    if i != 0:
      arr[count] = i
      count = count + 1
  
  for i in range(count, len(arr)):
    arr[i] = 0

  return arr




array =  lt=list(map(int,input('Enter the List').split()))
res = move_zero_to_end(array)
print(res)
